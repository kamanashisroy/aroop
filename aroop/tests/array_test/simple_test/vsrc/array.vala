
using aroop;

class test_array : Replicable {

	public static int main() {
		ArrayList<xtring> list = ArrayList<xtring>(16);
		xtring x = new xtring.copy_string("Fine");
		list[0] = x;
		list[0].pray(prayer.DESCRIBE);
		return 0;
	}
}
